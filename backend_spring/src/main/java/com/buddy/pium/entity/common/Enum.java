package com.buddy.pium.entity.common;

public class Enum {
    public enum Gender {
        M, F
    }

    public enum MateRequestStatus {
        PENDING,
        ACCEPTED,
        REJECTED
    }

}